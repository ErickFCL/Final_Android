<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Pokemon_Detalle">

    <ImageView
        android:id="@+id/imagenPokemon"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scaleType="fitXY"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/nombreP"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Nombre"
        android:textAlignment="center"
        android:textSize="25dp"
        app:layout_constraintTop_toBottomOf="@+id/imagenPokemon"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/tipoP"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Tipo"
        android:textAlignment="center"
        android:textSize="25dp"
        app:layout_constraintBottom_toTopOf="@+id/goP"
        app:layout_constraintTop_toBottomOf="@+id/nombreP"
        tools:ignore="MissingConstraints" />

    <Button
        android:id="@+id/capture"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/black"
        android:text="Copturar Pokemon"
        android:textSize="25dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tipoP"
        tools:ignore="MissingConstraints" />

    <Button
        android:id="@+id/goP"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/black"
        android:text="MOSTRAR UBICACION"
        android:textSize="25dp"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:ignore="MissingConstraints" />
</androidx.constraintlayout.widget.ConstraintLayout>